# v1 is a fully static client-side app and needs no environment variables.
# This file is a placeholder for future backend/offline additions, e.g.:
#
# VITE_API_URL=            # backend base URL (accounts / cross-device sync)
# VITE_SUPABASE_URL=       # if a hosted Postgres is added for shared progress
# VITE_SUPABASE_ANON_KEY=
